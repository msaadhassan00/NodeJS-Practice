const add = (x,y) => {
    return x + y;
}
const sub = (x,y) => {
    return x - y;
}
const mul = (x,y) => {
    return x * y;
}
const div = (x,y) => {
    return x / y;
}

module.exports = { add, mul, div, sub }
// module.exports = sub
// module.exports = mul
// module.exports = div